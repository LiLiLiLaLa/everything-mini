package com.every.everything.core.index.Impl;

import com.every.everything.core.index.FileIndex;
import com.every.everything.core.model.Thing;

public class FileIndexImpl implements FileIndex{
    //DAO

    @Override
    public void index(Thing thing) {

    }
}
